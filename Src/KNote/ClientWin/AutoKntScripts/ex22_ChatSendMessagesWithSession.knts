' Chat SingalR example 2, send messages with chat component

var i = 0;

var chat = GetKntChatCtrl();
chat.Run();

for i = 1 to 10
	chat.SendMessage("Hello " + i);
	printline "Hello " + i;
end for;

printline "-- END --";