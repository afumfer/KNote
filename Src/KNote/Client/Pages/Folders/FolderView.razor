@page "/folders/view/{FolderId}"

@attribute [Authorize(Roles = "Public, Staff, ProjecManager, Admin")]

<h3>FolderView</h3>

<p>FolderId: @FolderId.ToString()</p>

@code {
    [Parameter] public string FolderId { get; set; }
}
